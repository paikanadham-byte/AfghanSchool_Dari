{
  "name": "afghan-school-dari",
  "version": "2.0.0",
  "private": true,
  "description": "Afghan Care & School — offline-first PWA for schools, hospitals and clinics in Afghanistan (Dari / Pashto / English)",
  "main": "server.js",
  "scripts": {
    "start": "node --experimental-sqlite server.js",
    "dev": "node --experimental-sqlite server.js",
    "seed": "node --experimental-sqlite src/seed.js",
    "reset": "node --experimental-sqlite src/seed.js --reset",
    "crawl": "node crawler.js",
    "test": "node --experimental-sqlite src/selftest.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "fs-extra": "^11.1.1",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1"
  }
}
